<!DOCTYPE html>

<title>New Alerte</title>

{% block body %}
    <h1>Create new Alerte</h1>

    {{ include('alerte/_form.html.twig') }}

    <a href="{{ path('alerte_index') }}">back to list</a>
{% endblock %}
