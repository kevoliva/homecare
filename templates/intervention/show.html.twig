<!DOCTYPE html>

<title>Intervention</title>

{% block body %}
    <h1>Intervention</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervention.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ intervention.libelle }}</td>
            </tr>
            <tr>
                <th>TypeInterv</th>
                <td>{{ intervention.typeInterv }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ intervention.observation }}</td>
            </tr>
            <tr>
                <th>Remarque</th>
                <td>{{ intervention.remarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('intervention_index', {idBien:bien.id}) }}">back to list</a>

    <a href="{{ path('intervention_edit', {'id': intervention.id, idBien:bien.id}) }}">edit</a>

    {{ include('intervention/_delete_form.html.twig') }}
{% endblock %}
