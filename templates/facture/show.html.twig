{% extends 'proprietaire_base.html.twig' %}

{% block title %}
Mes factures
{% endblock %}

{% block contenu %}
<center>
  <h2>
    {{ facture.libelle }}
  </h2>
  <p>
    {{ facture.bien.getAdresse }}
    {{ facture.bien.getCodePostal }}
    {{ facture.bien.getVille }}
  </p>
</center>
<hr>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ facture.id }}</td>
    </tr>
    <tr>
      <th>Libelle</th>
      <td>{{ facture.libelle }}</td>
    </tr>
    <tr>
      <th>LaDate</th>
      <td>{{ facture.laDate ? facture.laDate|date('Y-m-d') : '' }}</td>
    </tr>
    <tr>
      <th>Facture</th>
      <td><a href="{{ asset('uploads/factures/' ~ facture.cheminFic) }}">Voir la facture</a></td>
    </tr>
  </tbody>
</table>
<br>

<a href="{{ path('facture_index', {idBien:bien.id}) }}">
  <button class="btn btn-info btn-sm">Retour</button>
</a>

<a href="{{ path('facture_edit', {'id': facture.id, idBien:bien.id}) }}" style="float:right">
<button class="btn btn-info btn-sm">Modifier</button>
</a>

<center>
{{ include('facture/_delete_form.html.twig') }}
</center>
{% endblock %}
